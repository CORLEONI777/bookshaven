"use client";
import {useState} from "react";
export default function CuratedListForm(){const [form,setForm]=useState({title:"",slug:"",intro_text:""});const [msg,setMsg]=useState("");async function save(e:React.FormEvent){e.preventDefault();setMsg("This UI is ready for the D1 mutation endpoint.");}return <form className="panel form-grid" onSubmit={save}><h2 className="field full">Create list</h2><div className="field full"><label>Title</label><input className="input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="field full"><label>Slug</label><input className="input" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})}/></div><div className="field full"><label>Intro text</label><textarea className="input textarea" value={form.intro_text} onChange={e=>setForm({...form,intro_text:e.target.value})}/></div><div className="field full"><button className="btn">Create</button><span className="muted">{msg}</span></div></form>}

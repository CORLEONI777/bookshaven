"use client";
import { useState } from "react";
export default function AdminBookForm({book}:{book:any}){const [form,setForm]=useState({id:book.id,title:book.title||"",description:book.description||"",why_this_book:book.why_this_book||"",source:book.source||"cached"});const [status,setStatus]=useState("");async function save(e:React.FormEvent){e.preventDefault();setStatus("Saving…");const r=await fetch("/api/admin/books",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});setStatus(r.ok?"Saved.":"Save failed.");}return <form className="panel form-grid" onSubmit={save}><div className="field full"><label>Title</label><input className="input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="field full"><label>Source</label><select className="input" value={form.source} onChange={e=>setForm({...form,source:e.target.value})}><option value="cached">cached</option><option value="curated">curated</option></select></div><div className="field full"><label>Description</label><textarea className="input textarea" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div className="field full"><label>Why this book — human-written editorial field</label><textarea className="input textarea" value={form.why_this_book} onChange={e=>setForm({...form,why_this_book:e.target.value})}/></div><div className="field full"><button className="btn">Save changes</button><span className="muted">{status}</span></div></form>}

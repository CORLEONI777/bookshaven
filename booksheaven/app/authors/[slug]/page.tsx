import Link from "next/link";
import { notFound } from "next/navigation";
import { queryBooks } from "@/lib/db";
export default async function AuthorPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=await queryBooks(`SELECT * FROM authors WHERE slug=? LIMIT 1`,slug);if(!a[0])notFound();const books=await queryBooks(`SELECT b.slug,b.title,b.cover_url FROM books b JOIN book_authors ba ON ba.book_id=b.id WHERE ba.author_id=? ORDER BY b.title`,Number(a[0].id));return <div className="container section"><div className="eyebrow">Author</div><h1>{String(a[0].name)}</h1><p className="prose">{String(a[0].bio||"")}</p><div className="section"><h2>Books</h2><div className="grid">{books.map(b=><Link className="card" href={`/books/${b.slug}`} key={String(b.slug)}><div className="cover">{b.cover_url?<img src={String(b.cover_url)} alt=""/>:<div className="cover-placeholder">{String(b.title)}</div>}</div><div className="card-body"><h3>{String(b.title)}</h3></div></Link>)}</div></div></div>}
